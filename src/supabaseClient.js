// SUPABASE DISABLED - Using mock data for demo purposes
// This file can be re-enabled when connecting to a real Supabase backend

/*
import { createClient } from '@supabase/supabase-js'

// Obtener las variables de entorno de Vite
const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY

// Crear y exportar el cliente de Supabase para usarlo en toda la aplicación
export const supabase = createClient(supabaseUrl, supabaseAnonKey)
*/
